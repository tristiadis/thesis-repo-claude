<!DOCTYPE html>
<html lang="<%= locale %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %><%= __('home.title') %></title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Alpine.js CDN -->
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <!-- Custom Tailwind Config -->
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#3b82f6',
            secondary: '#6366f1',
          }
        }
      }
    }
  </script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-white shadow-md sticky top-0 z-50">
    <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">

        <!-- Logo and Brand -->
        <div class="flex items-center">
          <a href="/" class="flex items-center space-x-3 hover:opacity-80 transition">
            <i class="fas fa-graduation-cap text-primary text-3xl"></i>
            <div class="flex flex-col">
              <span class="text-xl font-bold text-gray-900 hidden sm:block">Thesis Repository</span>
              <span class="text-xs text-gray-500 hidden sm:block">Digital Archive System</span>
            </div>
          </a>
        </div>

        <!-- Desktop Navigation -->
        <div class="hidden md:flex items-center space-x-8">
          <a href="/" class="text-gray-700 hover:text-primary font-medium transition">
            <i class="fas fa-home mr-2"></i><%= __('nav.home') %>
          </a>
          <a href="/browse/faculties" class="text-gray-700 hover:text-primary font-medium transition">
            <i class="fas fa-search mr-2"></i><%= __('nav.browse') %>
          </a>

          <!-- Language Switcher -->
          <div x-data="{ open: false }" class="relative">
            <button @click="open = !open" class="flex items-center space-x-1 text-gray-700 hover:text-primary font-medium transition">
              <i class="fas fa-globe mr-1"></i>
              <span class="uppercase text-sm"><%= locale %></span>
              <i class="fas fa-chevron-down text-xs"></i>
            </button>
            <div x-show="open" @click.away="open = false"
                 x-transition:enter="transition ease-out duration-200"
                 x-transition:enter-start="opacity-0 scale-95"
                 x-transition:enter-end="opacity-100 scale-100"
                 x-transition:leave="transition ease-in duration-150"
                 x-transition:leave-start="opacity-100 scale-100"
                 x-transition:leave-end="opacity-0 scale-95"
                 class="absolute right-0 mt-2 w-32 bg-white rounded-lg shadow-lg py-1 z-50">
              <a href="?lang=id" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 <%= locale === 'id' ? 'bg-blue-50 text-primary font-semibold' : '' %>">
                <i class="fas fa-check mr-2 <%= locale === 'id' ? '' : 'invisible' %>"></i>Indonesia
              </a>
              <a href="?lang=en" class="block px-4 py-2 text-gray-700 hover:bg-gray-100 <%= locale === 'en' ? 'bg-blue-50 text-primary font-semibold' : '' %>">
                <i class="fas fa-check mr-2 <%= locale === 'en' ? '' : 'invisible' %>"></i>English
              </a>
            </div>
          </div>

          <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
            <!-- Authenticated User -->
            <div x-data="{ open: false }" class="relative">
              <button @click="open = !open" class="flex items-center space-x-2 text-gray-700 hover:text-primary font-medium transition">
                <i class="fas fa-user-circle text-xl"></i>
                <span><%= user.name %></span>
                <i class="fas fa-chevron-down text-sm"></i>
              </button>

              <!-- Dropdown Menu -->
              <div x-show="open" @click.away="open = false"
                   x-transition:enter="transition ease-out duration-200"
                   x-transition:enter-start="opacity-0 scale-95"
                   x-transition:enter-end="opacity-100 scale-100"
                   x-transition:leave="transition ease-in duration-150"
                   x-transition:leave-start="opacity-100 scale-100"
                   x-transition:leave-end="opacity-0 scale-95"
                   class="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg py-2 z-50">
                <a href="<%= user.role === 'ADMIN' ? '/admin/dashboard' : '/student/dashboard' %>"
                   class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
                  <i class="fas fa-tachometer-alt mr-2"></i><%= __('nav.dashboard') %>
                </a>
                <a href="/profile" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
                  <i class="fas fa-user mr-2"></i><%= __('nav.profile') %>
                </a>
                <hr class="my-2">
                <a href="/auth/logout" class="block px-4 py-2 text-red-600 hover:bg-red-50">
                  <i class="fas fa-sign-out-alt mr-2"></i><%= __('nav.logout') %>
                </a>
              </div>
            </div>
          <% } else { %>
            <!-- Guest User -->
            <a href="/auth/login" class="bg-primary text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition font-medium">
              <i class="fas fa-sign-in-alt mr-2"></i><%= __('nav.login') %>
            </a>
          <% } %>
        </div>

        <!-- Mobile Menu Button -->
        <div class="md:hidden">
          <button @click="mobileMenuOpen = !mobileMenuOpen" class="text-gray-700 hover:text-primary">
            <i class="fas fa-bars text-2xl"></i>
          </button>
        </div>

      </div>

      <!-- Mobile Navigation -->
      <div x-show="mobileMenuOpen"
           x-data="{ mobileMenuOpen: false }"
           @click.away="mobileMenuOpen = false"
           class="md:hidden border-t border-gray-200 py-4">
        <a href="/" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
          <i class="fas fa-home mr-2"></i><%= __('nav.home') %>
        </a>
        <a href="/browse/faculties" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
          <i class="fas fa-search mr-2"></i><%= __('nav.browse') %>
        </a>

        <!-- Language Switcher (Mobile) -->
        <hr class="my-2">
        <div class="px-4 py-2">
          <div class="flex items-center gap-2">
            <i class="fas fa-globe text-gray-500"></i>
            <div class="flex-1 flex gap-2">
              <a href="?lang=id" class="flex-1 text-center px-3 py-1 rounded <%= locale === 'id' ? 'bg-primary text-white font-semibold' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' %>">
                ID
              </a>
              <a href="?lang=en" class="flex-1 text-center px-3 py-1 rounded <%= locale === 'en' ? 'bg-primary text-white font-semibold' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' %>">
                EN
              </a>
            </div>
          </div>
        </div>

        <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
          <hr class="my-2">
          <a href="<%= user.role === 'ADMIN' ? '/admin/dashboard' : '/student/dashboard' %>"
             class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
            <i class="fas fa-tachometer-alt mr-2"></i><%= __('nav.dashboard') %>
          </a>
          <a href="/profile" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">
            <i class="fas fa-user mr-2"></i><%= __('nav.profile') %>
          </a>
          <a href="/auth/logout" class="block px-4 py-2 text-red-600 hover:bg-red-50">
            <i class="fas fa-sign-out-alt mr-2"></i><%= __('nav.logout') %>
          </a>
        <% } else { %>
          <hr class="my-2">
          <a href="/auth/login" class="block px-4 py-2 text-primary hover:bg-blue-50 font-medium">
            <i class="fas fa-sign-in-alt mr-2"></i><%= __('nav.login') %>
          </a>
        <% } %>
      </div>
    </nav>
  </header>

  <!-- Flash Messages -->
  <%- include('../partials/flash') %>

  <!-- Main Content -->
  <main class="flex-grow">
    <%- body %>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white mt-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">

        <!-- About Section -->
        <div class="col-span-1 md:col-span-2">
          <div class="flex items-center space-x-3 mb-4">
            <i class="fas fa-graduation-cap text-primary text-3xl"></i>
            <span class="text-xl font-bold">Thesis Repository</span>
          </div>
          <p class="text-gray-400 mb-4">
            A comprehensive digital archive system for academic theses and dissertations.
            Preserving knowledge for future generations.
          </p>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-400 hover:text-primary transition">
              <i class="fab fa-facebook text-xl"></i>
            </a>
            <a href="#" class="text-gray-400 hover:text-primary transition">
              <i class="fab fa-twitter text-xl"></i>
            </a>
            <a href="#" class="text-gray-400 hover:text-primary transition">
              <i class="fab fa-linkedin text-xl"></i>
            </a>
            <a href="#" class="text-gray-400 hover:text-primary transition">
              <i class="fab fa-instagram text-xl"></i>
            </a>
          </div>
        </div>

        <!-- Quick Links -->
        <div>
          <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
          <ul class="space-y-2">
            <li><a href="/" class="text-gray-400 hover:text-primary transition">Home</a></li>
            <li><a href="/browse" class="text-gray-400 hover:text-primary transition">Browse Theses</a></li>
            <li><a href="/about" class="text-gray-400 hover:text-primary transition">About Us</a></li>
            <li><a href="/contact" class="text-gray-400 hover:text-primary transition">Contact</a></li>
          </ul>
        </div>

        <!-- Resources -->
        <div>
          <h3 class="text-lg font-semibold mb-4">Resources</h3>
          <ul class="space-y-2">
            <li><a href="/help" class="text-gray-400 hover:text-primary transition">Help Center</a></li>
            <li><a href="/guidelines" class="text-gray-400 hover:text-primary transition">Submission Guidelines</a></li>
            <li><a href="/faq" class="text-gray-400 hover:text-primary transition">FAQ</a></li>
            <li><a href="/privacy" class="text-gray-400 hover:text-primary transition">Privacy Policy</a></li>
          </ul>
        </div>
      </div>

      <!-- Copyright -->
      <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
        <p><%= __('footer.copyright', new Date().getFullYear()) %></p>
        <% if (typeof stats !== 'undefined' && stats) { %>
          <p class="text-sm mt-2"><%= stats.totalTheses.toLocaleString() %> <%= __('home.total_theses').toLowerCase() %> · <%= stats.totalDownloads.toLocaleString() %> <%= __('home.total_downloads').toLowerCase() %></p>
        <% } %>
      </div>
    </div>
  </footer>

  <!-- Back to Top Button -->
  <button @click="window.scrollTo({top: 0, behavior: 'smooth'})"
          x-data
          x-show="window.pageYOffset > 300"
          x-transition
          class="fixed bottom-8 right-8 bg-primary text-white p-3 rounded-full shadow-lg hover:bg-blue-700 transition z-50">
    <i class="fas fa-arrow-up"></i>
  </button>

</body>
</html>
